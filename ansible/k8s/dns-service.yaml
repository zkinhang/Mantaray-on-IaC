apiVersion: v1
kind: Service
metadata:
  name: rov-streamer-service
spec:
  type: ClusterIP
  selector:
    app: http-streamer-a
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
---
apiVersion: v1
kind: Service
metadata:
  name: rov-cam-streamer-service 
spec:
  type: ClusterIP
  selector:
    app: http-streamer-b
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000