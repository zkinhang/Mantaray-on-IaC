# Mantaray Parameter Configurator API

Lightweight Flask API for managing ROV parameters, natively supporting the React Control Interface.

## Local Development (with `uv`)

1. Install dependencies and create a virtual environment:
   ```bash
   uv sync
   ```

2. Run the application:
   ```bash
   uv run python app.py
   ```
